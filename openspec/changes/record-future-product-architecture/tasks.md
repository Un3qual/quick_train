## 1. Preserve Deferred Architecture

- [x] 1.1 Move the approved future Forms, Projects, Tasks, Finance, and Reputation decisions into this documentation-only OpenSpec record and mark capability specs as intentionally skipped.

- [x] 1.2 Record the approved opaque-file MVP boundary and future format-aware rendering/HTTP storage work without implementing it.

- [x] 1.3 Link the dedicated `add-project-task-collection` proposal and record its later-enrollment choice while retaining Finance, Reputation, and media/storage as separate scope. Core implementation is tracked in that dedicated change.

- [x] 1.4 Split image-dependent collection into `add-project-task-media` after detailed media support; make core collection independently implementable and archivable with text spans and opaque asset/export contracts.
