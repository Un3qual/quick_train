## 1. Tooling and Product Authorization Foundation

- [ ] 1.1 Use Igniter and the available Ash generators to add the `QuickTrain.Assets` and `QuickTrain.Datasets` domain skeletons, retaining only the generated structure needed for these product domains.
- [ ] 1.2 Select and exactly pin a stable GA Oban release through the repository toolchain, update `.mise.toml`, `mix.exs`, and `mix.lock` together, and run the dependency's supported generator to add and review the Oban jobs-table migration before enabling workers.
- [ ] 1.3 Add the product capability keys for asset read/management and dataset read/management/import; make every organization-scoped authorization decision require the organization itself to be active; and add a responsibility-named operator-only Mix task that transactionally and idempotently bootstraps an exact existing active user, active organization, active membership, manager role assignment, and initial grants without exposing bootstrap through GraphQL.
- [ ] 1.4 Before enabling canonical issuer-only lookup, migrate every legacy `provider == "oidc"` identity to the one configured verified issuer URI and abort without partial changes on missing/multiple issuer configuration or identity conflicts. Complete the OIDC authentication handshake with unauthenticated begin/exchange actions that generate state, nonce, and a standards-compliant PKCE verifier server-side from at least 256 bits of CSPRNG entropy each, persist only a unique collision-checked state hash, nonce hash, and server-side verifier material, derive only S256, reject caller-supplied proof material, and atomically claim each unexpired login transaction through a one-way pending-to-exchanging transition before provider exchange. Validate provider signature, issuer, audience, nonce, and expiry; support only fail-closed `issuer_subject_only` linking; require verified email only to create a brand-new active identity and user; derive the required display name from the first nonblank trimmed `name` or `preferred_username` claim with the normalized verified email local part as deterministic fallback; lock and recheck that every matched existing identity and linked global user are exactly active in the session-issuance transaction; keep lifecycle status out of provider claim refresh; never link, merge, or reassign by email or update an existing identity's `user_id`; reject missing/unknown policy, disabled or unknown identity/user status, and identity conflicts; then commit one high-entropy opaque bearer-session issuance with the final consumed transition and persist only the token's SHA-256 hash and session metadata.
- [ ] 1.5 Refine the Session resource, snapshot, and migration by first revoking every legacy null-hash session and assigning each a unique reserved `legacy-revoked:<session-id>` value that bearer hashing cannot produce, then require every issued bearer session to have a non-null token hash protected by a unique Ash identity and database index. Implement indexed bearer-session resolution in the Phoenix API pipeline so GraphQL requests hash the presented token and receive an unexpired, unrevoked session's active global `User` as both the Absinthe and Ash actor, with fail-closed handling for missing, invalid, expired, revoked, or inactive sessions.
- [ ] 1.6 Remove the GraphQL `health` field, retain operational health only at `/healthz`, restrict GraphiQL to development, and remove or isolate policy-disabled foundation operations so the OIDC begin/exchange handshake is the only unauthenticated GraphQL behavior.
- [ ] 1.7 Add a retention cutoff to OIDC login transactions, implement a responsibility-named Accounts cleanup worker that idempotently deletes consumed, expired, and abandoned transactions only after a fixed replay-rejection interval while preserving every unexpired pending or exchanging transaction, and register it with Oban's supported fixed-interval periodic scheduler using job uniqueness that prevents overlapping runs.
- [ ] 1.8 Add focused integration tests proving server-generated CSPRNG state, nonce, and S256 PKCE with caller-supplied proof rejection, successful one-time bearer issuance, atomic rejection of simultaneous exchanges for one OIDC state over independent connections, immutable issuer/subject linking, conflict-safe legacy `oidc` provider-key migration, verified-email-only new-account creation with deterministic display-name fallback, denial of disabled or unknown-status identities and active identities linked to disabled or unknown-status users without provider-driven reactivation, fail-closed linking configuration and conflicts, safe staged migration of legacy null-hash sessions, no raw-token persistence, the non-null unique token-hash identity and indexed lookup path, periodic cleanup enqueueing without manual invocation, expired/consumed login-state cleanup without live-state deletion or replay, unauthenticated GraphQL health absence, denial of non-handshake operations, production/test denial of `/graphiql` with `/healthz` preserved, active-session actor propagation, active-organization and explicit-capability enforcement, first-manager bootstrap atomicity/idempotency, and cross-organization non-disclosure before exposing product actions.

## 2. Immutable Asset Domain

- [ ] 2.1 Generate the Asset resource, snapshot, and migration, then refine its organization ownership, lifecycle, SHA-256 hash represented as exactly 64 lowercase hexadecimal characters across registration, persistence, deduplication, and adapters, byte size, media type, image dimensions, staging and sealed internal storage keys, staging expiry, nullable staging-cleaned timestamp, a partial ready-only organization/content-hash identity, constraints, and indexes including the expired-and-not-cleaned staging scan.
- [ ] 2.2 Define the narrow `QuickTrain.Assets.Storage` behavior and implement deterministic development and test adapters for staging upload registration, one idempotent verify-and-seal operation that pins a staging version or fences writes and verifies the sealed object, redundant-object and expired-staging cleanup, and short-lived read access without exposing persistent credentials or locations; require authenticated encrypted transport, reject insecure descriptors and redirects, and apply no-store/no-referrer handling to credential-bearing access.
- [ ] 2.3 Implement policy-protected asset registration and access actions that derive organization scope relationally, return only provider-neutral access descriptors, reuse a canonical ready asset only when hash, byte size, and media type all match, and otherwise return `asset_identity_conflict`.
- [ ] 2.4 Implement asset finalization as an idempotent state transition that records readiness only from SHA-256 hash, size, safely identified actual media type, and positive image dimensions computed over the same pinned immutable sealed object later served; reject declared/actual media-type mismatches and active document formats including HTML, XHTML, and SVG even under a benign declaration; reject an unguarded verify-then-copy sequence; record sanitized failures; permit a new registration after failure; and converge concurrent identical finalizations on one metadata-compatible canonical ready asset.
- [ ] 2.5 Implement responsibility-named asset verification and staging-cleanup workers with transactional/idempotent enqueue paths, fixed expiry/grace behavior for abandoned pending, failed, `duplicate_content`, and sealed registrations, strict preservation of sealed objects, and clear behavior when production storage is not configured. Register staging cleanup with Oban's supported fixed-interval periodic scheduler, scan the indexed expired rows whose staging-cleaned timestamp is null, and use job uniqueness to prevent overlapping runs. After the adapter confirms deletion or absence, lock the asset and record cleanup completion so successful rows are excluded from later scans while pre-marker retries remain idempotent.
- [ ] 2.6 Expose only the deliberate asset registration, finalization, authorized-access, and scoped-read operations through GraphQL.
- [ ] 2.7 Add focused resource, policy, storage-adapter, worker, and GraphQL tests for successful finalization, canonical SHA-256 representation, rejection of HTML/XHTML/SVG and falsely declared active bytes, a concurrent staging overwrite between verification and sealing, post-finalization upload replay, mismatch followed by correct re-registration, canonical reuse with matching facts, conflicting facts for an existing hash, concurrent identical finalization, automatic periodic cleanup of an unfinalized expired registration without manual worker invocation, pending/failed/`duplicate_content`/sealed staging cleanup, completed-cleanup scan exclusion, safe retry before the cleanup marker commits, overlap-preventing job uniqueness, sealed-object preservation, immutable reads, secure descriptor and redirect rejection, adapter failure, inactive-organization denial, and cross-organization access denial.

## 3. Versioned Dataset Schemas

- [ ] 3.1 Generate Dataset, DatasetSchemaVersion, DatasetRecordType, and DatasetFieldDefinition resources with snapshots and migrations, then refine their relationships, organization ownership, exactly-one designated root type per published first-release schema version, dataset/schema/record-type composite identities and foreign keys, constraints, and indexes.
- [ ] 3.2 Implement draft-only schema editing actions for the initial text, integer, decimal, boolean, UTC date-time, and asset value families; every record-type or field create, update, or delete transaction must lock and recheck the parent schema version before writing, in addition to enforcing unique field keys, requiredness, and the sole first-release `single` cardinality.
- [ ] 3.3 Implement schema publication as a transaction that takes the same parent-version lock as child edits, rechecks draft state, validates the complete schema graph, requires exactly one root record type for the first release, and makes the published version structurally immutable.
- [ ] 3.4 Implement creation of a new draft version from an existing published schema without mutating or re-pointing historical versions.
- [ ] 3.5 Add fail-closed Ash policies and deliberate GraphQL lifecycle/read actions for datasets and schema versions; do not expose generic mutations for published structures.
- [ ] 3.6 Add focused resource, policy, concurrency, and GraphQL tests for valid publication, duplicate or invalid fields, rejection of non-single cardinality, an independent-connection child-edit/publication race, immutable published versions, version succession, inactive-organization denial, and cross-organization denial.

## 4. Normalized Item Revisions and Typed Values

- [ ] 4.1 Generate DatasetItem, DatasetItemRevision, DatasetRecord, and DatasetValue resources with stable item identities, optional dataset-scoped external keys, same-dataset schema pins, exact record-type field scope, designated-root revision scope backed by composite identities and foreign keys, revision numbers, schema-aware fingerprints, field occurrence ordinals, import-row candidate-record references, snapshots, migrations, constraints, and indexes.
- [ ] 4.2 Generate the six typed child resources for text, integer, decimal, boolean, UTC date-time, and asset values, then refine their one-to-one relationships and family-specific constraints.
- [ ] 4.3 Implement the narrow deferred PostgreSQL constraint trigger through an AshPostgres migration so every DatasetValue has exactly one typed child matching its field family at transaction commit.
- [ ] 4.4 Implement one private transactional record-construction workflow that requires first-release revision candidates to use the pinned published schema's designated root record type, resolves fields only within the candidate record's exact type, requires exactly one occurrence for required single fields and zero-or-one for optional single fields, rejects repeated, sibling-type, unknown, and nested input explicitly, and creates no partial record graph.
- [ ] 4.5 Implement one shared versioned, domain-separated, length-prefixed canonical typed encoder for revision and import-request fingerprints, with schema/root identities; field-identity/ordinal ordering; canonical text, integer, decimal, boolean, UTC-microsecond, and asset encodings; and 64-character lowercase hexadecimal SHA-256 output. Use it in stable-item revision creation after the atomic dataset/external-key get-or-create, uniqueness-conflict reload, and authoritative item-row lock for unchanged-content-and-schema detection and monotonic immutable revision assignment.
- [ ] 4.6 Validate asset-valued fields within candidate-record construction and revision transactions so only ready assets owned by the same organization can be referenced without leaking unauthorized metadata; treat every such asset as compatible with an asset-family field in this release.
- [ ] 4.7 Add paginated, typed GraphQL reads for items and revisions that return schema, record, field, ordinal, and concrete typed-value identity without exposing unrestricted value mutation.
- [ ] 4.8 Add focused tests for every value family, required and optional occurrence counts, repeated and unknown fields, sibling-record-type field rejection, non-root revision-candidate rejection through both Ash actions and database constraints, type mismatches, unsupported nested values, typed-child commit constraints, cross-dataset schema rejection, historical immutability, canonical fingerprint convergence for field order, decimal scale, UTC offsets, and occurrence order, divergence for real schema/field/ordinal/family/value changes, unchanged same-schema fingerprints, identical values across schema versions creating a revision, concurrent first-item creation, concurrent revision ordering, and first-release asset compatibility.

## 5. Idempotent Dataset Imports

- [ ] 5.1 Generate DatasetImport and DatasetImportRow resources with snapshots and migrations, then refine same-dataset organization/schema/actor relationships, the `open` plus derived post-finalization lifecycle states, caller-stable row key, persisted customer external key, unique source position, normalized candidate-record reference, request fingerprint, processing start and lease expiry, attempt count, terminal outcome, result references, sanitized errors, identities, counters, and indexes, including a partial import/external-key identity for non-null keys.
- [ ] 5.2 Implement the policy-protected open-import action with a unique organization/dataset/idempotency-key identity, fingerprint coverage of schema and immutable parameters, and atomic create-or-return behavior for concurrent identical requests while changed requests fail with `idempotency_conflict`.
- [ ] 5.3 Implement bounded append-row actions that lock the import and recheck it is `open`, use import-scoped row-key and source-position identities plus the shared canonical request fingerprint encoding with explicit null/present markers over the persisted external key, source position, normalized typed record stream, and all immutable input, atomically return already accepted matching rows without reprocessing, reject changed retries, reject a competing row for an already accepted import/external-key identity with `duplicate_external_key` before persisting or counting a second row, persist valid input as immutable normalized candidate records of the pinned schema's designated root type with fields from that exact type, and persist accepted invalid row outcomes without opaque payload or JSONB staging columns.
- [ ] 5.4 Process valid candidate records through the canonical item-revision workflow so new, changed, and unchanged rows produce the specified atomic outcome and provenance; retain append-validation failures as terminal rows that workers skip.
- [ ] 5.5 Implement import finalization as the authorization point for a durable organization-scoped command: lock the same import row used by append, atomically transition it out of `open` before enqueueing, and implement the exact lifecycle precedence and disjoint direct counts for empty, active, retryable, all-success, all-failed, and mixed sealed batches. `accepted` counts every persisted row; pending/processing/succeeded/unchanged count exact outcomes; failed counts persisted append-validation and processing failures; and pre-persistence `duplicate_external_key` rejections affect no row count. Claim rows under lock with processing-start, lease-expiry, and incremented attempt-count updates that return the attempt as a fencing token.
- [ ] 5.6 Implement the responsibility-named import worker with idempotent job insertion, immutable batch-scope enforcement, terminal-row and active-lease skipping, expired-lease reclamation, a snoozed or transactionally unique follow-up scheduled for the earliest active lease expiry before a skipping worker exits, terminal writes conditioned on the row still owning the claimed attempt, normalized candidate loading by identity rather than job payload, short item-lock transactions, and safe recovery after transient failures. Do not reauthorize the initiating user's mutable membership or the organization's later active state after an already authorized finalization; organization deactivation denies caller access but does not implicitly cancel accepted work.
- [ ] 5.7 Expose only the deliberate open, append, finalize, inspect, and paginated outcome operations through authenticated GraphQL.
- [ ] 5.8 Add focused action, policy, worker, GraphQL, and independent-connection concurrency tests for concurrent and sequential batch create-or-return, canonical row-fingerprint convergence and real-change divergence, identical and conflicting row retries including changed external keys, duplicate external-key rejection without count changes, persisted append-validation and processing failures in the failed count, append/finalization ordering, the complete lifecycle/count truth table, worker crash after claim, active-lease exclusion with automatic scheduled recovery, stale-lease recovery, rejection of a resumed stale worker's fenced terminal write, durable finalized scope after actor or organization deactivation with caller access denied, unseen-external-key and existing-item races, cross-dataset schemas, and unauthorized, active-content, falsely declared, or cross-organization asset references.

## 6. Integration and Delivery Gates

- [ ] 6.1 Register the new domains, repositories, supervisors, responsibility-named authentication/asset/import workers, the fixed-interval unique schedules for OIDC transaction and asset staging cleanup, and GraphQL types through the existing top-level application boundaries without introducing a generic service or direct-Ecto product API.
- [ ] 6.2 Review the staged legacy identity and session data migrations, generated Oban and AshPostgres migrations, and Ash snapshots together, verifying conflict-safe `oidc`-to-issuer mapping before canonical lookup; revoked unique reserved values before the session token-hash non-null identity; the jobs table; OIDC retention indexes; composite same-dataset, exact-record-type, and designated-root-type foreign keys; ready-only asset uniqueness; canonical SHA-256 constraints; staging expiry and cleanup completion; batch and row idempotency identities; import/external-key uniqueness; row lease attempts and fences; deferred typed-value enforcement; lifecycle constraints; and indexes for organization scope, external keys, latest revisions, record fields, asset cleanup, and import processing.
- [ ] 6.3 Exercise the complete workflow in an integration test: complete the OIDC-to-bearer handshake to create a global user, run the operator-only first-manager bootstrap for that exact user, create and publish a schema, register/finalize a pinned sealed asset, import mixed rows, retry batch and row requests idempotently, recover a stale row claim, and query typed historical revisions under active organization authorization.
- [ ] 6.4 Run formatter, focused tests, migration checks, and `mise run openspec.validate`, resolving all warnings or artifact drift.
- [ ] 6.5 Run `mise run verify` from a clean migrated test database and record required production configuration for the exact `issuer_subject_only` linking policy, first-manager bootstrap, asset storage, and the fixed-interval OIDC-retention and asset-staging Oban cleanup schedules without selecting a storage or payment provider.
